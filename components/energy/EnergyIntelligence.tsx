"use client";

import { EnergyWorkflow } from "./EnergyWorkflow";

/** EnergyAI is organized as a 5-step decision workflow:
 *  Monitor → Insight → AI Analysis → AI Recommendation & Action → Report. */
export function EnergyIntelligence() {
  return <EnergyWorkflow />;
}
